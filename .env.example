# Copy this file to .env before running Docker Compose.
# Choose a strong local password. The .env file is ignored by Git.
NEO4J_PASSWORD=replace-with-a-strong-local-password

# Optional. The visual analytics application works without Gemini features.
GEMINI_API_KEY=
